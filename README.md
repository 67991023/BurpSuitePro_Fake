# 🔥 OpenBurp (Educational Web Scanner)

**BurpSuitePro_Fake** คือเครื่องมือทดสอบความปลอดภัยเว็บไซต์ (Web Vulnerability Scanner) ที่เขียนด้วยภาษา Go พัฒนาขึ้นเพื่อการศึกษาการทำงานของ Proxy, Crawler และ Automated Scanner



## ✨ ฟีเจอร์หลัก (Features)
* **Intercept Proxy:** ดักจับ Traffic HTTP/HTTPS และแก้ไข Request ได้
* **Auto Discovery:** มี Crawler และ Fuzzer ช่วยค้นหา Link และ Path ลับบนเว็บเป้าหมาย
* **Vulnerability Scanner:** ยิงทดสอบ SQL Injection และ XSS อัตโนมัติ
* **Repeater:** เครื่องมือสำหรับแก้ไขและยิง Request ซ้ำ (Manual Pentest)
* **Real-time Dashboard:** ดูผลการสแกนผ่านหน้าเว็บ

---

## 🛠️ สิ่งที่ต้องมี (Prerequisites)
* **Go (Golang):** เวอร์ชั่น 1.18 ขึ้นไป
* **Git:** สำหรับดาวน์โหลดโค้ด

---

## 📥 การติดตั้ง (Installation)

1.  **Clone โปรเจกต์:**
    ```bash
    git clone [https://github.com/67991023/BurpSuitePro_Fake.git](https://github.com/67991023/BurpSuitePro_Fake.git)
    cd BurpSuitePro_Fake
    ```

2.  **ติดตั้ง Libraries:**
    ```bash
    go mod tidy
    ```

3.  **สร้าง Certificate (run ครั้งแรกครั้งเดียว):**
    เพื่อให้โปรแกรมดักจับ HTTPS ได้ เราต้องสร้างไฟล์ `ca.crt` และ `ca.key` ก่อน
    ```bash
    cd phase1_proxytest
    ###เอาคอมเมนท์ออก
    go run gencert.go
    ```
    *เมื่อรันเสร็จ คุณจะได้ไฟล์ `ca.crt` ให้ทำการ Install ลงใน Browser (Trusted Root CA)*

---

## 🎯 วิธีเปลี่ยนเป้าหมาย (Configuration)

เนื่องจากโปรเจกต์นี้เน้นความปลอดภัย จึงจำกัดการสแกนไว้เฉพาะโดเมนที่ระบุ (Scope) คุณต้องแก้ไขไฟล์ `main.go` เพื่อกำหนดเป้าหมายใหม่:

1.  เปิดไฟล์ `main.go`
2.  แก้ไข **2 จุด** ดังนี้:

**จุดที่ 1: กำหนด Scope**
ป้องกันไม่ให้สแกนหลุดไปโดเมนอื่น
```go
// 🎯 เป้าหมายหลัก (แก้ตรงนี้ตามหน้างาน)
targetDomain = "example.com"  // <-- ใส่แค่ชื่อโดเมน

จุดที่ 2: กำหนด URL เริ่มต้น (ในฟังก์ชัน main บรรทัด ~80) จุดเริ่มต้นให้ Crawler ทำงาน
// 🕸️ PHASE 6: AUTO-DISCOVERY MODULE
// ...
target := "[http://example.com/login](http://example.com/login)" // <-- ใส่ URL เต็มๆ

บันทึกไฟล์แล้วรันโปรแกรมใหม่

วิธีใช้งาน (Usage)
รันโปรแกรม:

Bash
go run .
เปิด Dashboard: ไปที่ 👉 http://localhost:8081/dashboard

ตั้งค่า Proxy: ตั้งค่า Browser ให้วิ่งผ่าน Proxy:

IP: 127.0.0.1

Port: 8080

เริ่มเจาะระบบ: เข้าเว็บเป้าหมายผ่าน Browser โปรแกรมจะเริ่มเก็บ Log และยิงสแกนหาช่องโหว่ให้อัตโนมัติ

⚠️ คำเตือน (Disclaimer)
โปรแกรมนี้สร้างขึ้นเพื่อ การศึกษา (Educational Purpose) เท่านั้น ผู้พัฒนาไม่รับผิดชอบต่อความเสียหายใดๆ ที่เกิดจากการนำไปใช้โจมตีเว็บไซต์ที่ไม่ได้รับอนุญาต